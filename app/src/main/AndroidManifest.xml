<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="opengl.glexamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".glActivity.TextureTriangleActivity" />
        <activity android:name=".glActivity.IDCardActivity" />
        <activity android:name=".glActivity.CreateIDCardActivity" />
        <activity android:name=".glActivity.SkyBoxActivity" />
        <activity
            android:name=".glActivity.LightOpenglActivity"
            android:label="@string/title_activity_light_opengl"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <!--
             ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".glActivity.CurlActivity"
            android:label="@string/title_activity_curl"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>
    </application>

</manifest>
